// Reutiliza la misma lógica que el panel empleado.
// El page consulta por auth.uid() → funciona para cualquier rol con entrada en "empleados".
export { default } from "@/app/dashboard/empleado/perfil/page";
